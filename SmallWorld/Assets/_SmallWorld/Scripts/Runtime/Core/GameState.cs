namespace SmallWorld.Core
{
    /// <summary>High-level application states shared by scenes and UI.</summary>
    public enum GameState
    {
        Boot,
        MainMenu,
        Loading,
        Playing,
        Paused
    }
}
